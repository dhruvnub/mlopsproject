# params.yaml — DVC parameter tracking
# Change values here and run: dvc repro
# DVC detects changes and reruns automatically

model:
  n_estimators: 150
  max_depth: 8
  test_size: 0.2
  random_state: 42
